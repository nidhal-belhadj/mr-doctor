const Footer = () => {
  const footerStyle = {
    padding: "0 20px",
    lineHeight: "1.5em",
    display: "flex",
    justifyContent: "space-between",
  };

  return (
    <footer style={footerStyle}>
      <div>DR. Olivier AMI</div>
      <div>LINKS</div>
    </footer>
  );
};

export default Footer;
